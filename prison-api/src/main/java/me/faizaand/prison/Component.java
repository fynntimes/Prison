package me.faizaand.prison;

/**
 * A component performs one task in the plugin.
 * They are essentially independent of each other, aside from the APIs that they choose to expose.
 * They provide modularity to the system so that users can toggle features at will.
 */
public interface Component {
}
